cmake_minimum_required(VERSION 3.22)

PROJECT(HELLO)

ADD_SUBDIRECTORY(src bin)

SET(SHARED $ENV{HOME}/learn_cpp/cmake/04/share/)

INSTALL(FILES COPYRIGHT README DESTINATION ${SHARED})
INSTALL(PROGRAMS runhello.sh DESTINATION ${SHARED})
INSTALL(DIRECTORY doc/ DESTINATION ${SHARED}/doc)